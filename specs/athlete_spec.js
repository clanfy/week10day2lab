var athlete = require ('../athlete');
var assert = require("assert");

describe("Athlete", function(){
  it("should be hydrated to 100", function(){
    assert.equal(100, athlete.hydration);
  });
  it("distance should start at 0 ")
});